- zobrist hashing + transposition table
- are we handling eval correctly in our tables? i.e. are we accidentally negating stuff?
- fix quiescence search
- rewrite bot api
- threefold detection
- 50 move rule detection
- dangerous code (casting `void *` into `Board *`)